<h1>Games#index</h1>
<% @games.each do |game| %>
  <div class="game">
    <h2><%= game.title %></h2>
    <p><%= game.description %></p>
  </div>
<% end %>
<%= link_to 'New Game', new_game_path, class: 'btn btn-primary' %>
